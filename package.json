{
  "name": "quiet-notes",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter quiet-notes-functions... --filter quiet-notes-web... build"
  },
  "engines": {
    "node": ">=16",
    "pnpm": ">=7"
  },
  "devDependencies": {
    "vitest": "0.31.1"
  }
}
